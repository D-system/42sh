echo Welcome_on_42sh._Please_report_bug.
# I love myself !
# User's binaries
#set path=($HOME/{sbin,bin})
# School's (Bocal) binaries
#set path=($path /usr/school/{sbin,bin})
# Package's binaries
#set path=($path /usr/local/{sbin,bin})
# System's binaries
#set path=($path /usr/{bin,sbin} /{sbin,bin})
#set prompt=prompt_de_ouf
#setenv SHELL "/bin/tcsh"
#setenv TERM "xterm"
#setenv PAGER "less"
#setenv EDITOR "emacs"
#setenv MAIL "/u/all/$USER/mail/$USER"
#setenv TOTO "tata"
#set watch=(1 any any)
set history=200
#set savehist=10
#set toto=tata
## shell
alias l ls
alias ll ls
alias la ls
# EOF
