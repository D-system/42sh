# I love myself !

# User's binaries
#set path=($HOME/{sbin,bin})
# School's (Bocal) binaries
#set path=($path /usr/school/{sbin,bin})
# Package's binaries
#set path=($path /usr/local/{sbin,bin})
# System's binaries
#set path=($path /usr/{bin,sbin} /{sbin,bin})

#set prompt="<test for 42>"
#set rprompt="[ %B%~%b ]
#setenv SHELL "/bin/tcsh"
#setenv TERM "xterm"
#setenv PAGER "less"
#setenv EDITOR "emacs"
#setenv MAIL "/u/all/$USER/mail/$USER"
#setenv TOTO "tata"
#set watch=(1 any any)
#set history=5000
#set savehist=10
#set toto=tata


## shell
#alias salut "echo coucou"
#alias 42 "echo 42sh is the best sh"

# EOF
